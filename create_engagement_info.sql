use kitt_sql_review;

create table engagement_info (
ID int,
client_name varchar(255),
engagement_code varchar(255),
creater varchar(50),
master_subquery varchar(10),
versions int,
file_names varchar(50),
reviewers varchar(255),
uploadtime datetime

);